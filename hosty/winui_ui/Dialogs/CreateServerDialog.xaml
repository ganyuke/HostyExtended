<ContentDialog
    x:Class="winui_ui.Dialogs.CreateServerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:winui_ui.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Create Server"
    PrimaryButtonText="Next"
    CloseButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    CloseButtonClick="ContentDialog_CloseButtonClick"
    DefaultButton="Primary">

    <Grid Width="400" MinHeight="300">
        <!-- Configuration Step -->
        <StackPanel x:Name="ConfigPanel" Spacing="16">
            <TextBox x:Name="NameBox" Header="Server Name" PlaceholderText="My Awesome Server" TextChanged="Input_Changed" />
            
            <ComboBox x:Name="VersionBox" Header="Minecraft Version" HorizontalAlignment="Stretch" SelectionChanged="Input_Changed">
                <ComboBoxItem Content="Loading versions..." />
            </ComboBox>

            <TextBlock x:Name="LoaderText" Text="Fabric Loader: Checking..." Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />

            <StackPanel Spacing="8">
                <TextBlock Text="RAM Allocation" Style="{StaticResource BodyTextBlockStyle}" />
                <Slider x:Name="RamSlider" Minimum="1024" Maximum="16384" StepFrequency="1024" Value="4096" ValueChanged="RamSlider_ValueChanged" />
                <TextBlock x:Name="RamValueText" Text="4096 MB (4 GB)" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
            </StackPanel>
            
            <ToggleSwitch x:Name="OptimiseToggle" Header="Install performance mods" IsOn="False" />
        </StackPanel>

        <!-- Progress Step -->
        <StackPanel x:Name="ProgressPanel" Spacing="16" Visibility="Collapsed" VerticalAlignment="Center">
            <SymbolIcon Symbol="Download" />
            <TextBlock x:Name="ProgressTitle" Text="Creating server..." Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
            <ProgressBar x:Name="InstallProgressBar" Minimum="0" Maximum="100" Value="0" />
            <TextBlock x:Name="ProgressSubtitle" Text="Preparing..." Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
        </StackPanel>
    </Grid>
</ContentDialog>
